<VirtualHost *:80>
	# ServerName k8swp.blast.co.id

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/html

	# Ketersedian loglevel: trace8, ..., trace1, debug, info, notice, warn,
	# error, crit, alert, emerg.
	# Sangat memungkinkan untuk mengkonfigurasi loglevel untuk
	# modules khusus, misal
	#LogLevel info ssl:warn

	# ErrorLog /dev/stderr
	# CustomLog /dev/stdout combined
</VirtualHost>
